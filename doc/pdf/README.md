## Печатная форма карточки контрагента:

Получить pdf карточки.

**URL** : `/pdf`

**Обязательные параметры** :
- `inn(str) or ogrn(str)` - ИНН или ОГРН контрагента

**Необязательные параметры** :
- `kpp(str)` - КПП контрагента

**Метод** : `GET`

**Формат ответа**

```json
bytearray
```